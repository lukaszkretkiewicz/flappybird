#include "Game.hpp"
int main() {
  // event loop
  // ecs

  // data driven
  // entt - tworzy rejestr, komponenty s dodawane do encji, w jaki sposob sa
  //    sf::RenderWindow window(sf::VideoMode(512,512),"dhd", sf::Style::Close |
  //    sf::Style::Resize);
  Game game;
  game.run();

  
  return 0;
}